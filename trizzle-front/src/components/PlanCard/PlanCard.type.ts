export type PlanCardProps = {
  userId: string;
  planId: number;
  thumbnail: string;
  title: string;
  region: string;
  startDate: string;
  endDate: string;
  likeCount: number;
  commentCount: number;
  thema: string[];
};